let username = "Dev";
let password = "secure123";
let isAccountLocked = "true";

if ((username === "Dev" && password === "secure123") && isAccountLocked === "false") {
    console.log("allow to enter");
} else {
    console.log("not allowed to enter");
}
