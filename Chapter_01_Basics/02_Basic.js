const { use } = require("react");

//"a" is identifier know as variable which store data in it
let a = 10;
console.log(a);

let b = 20;
console.log(b);

// Without the semicolon we can execute the code but we have to use for the good practicelet 
let c = 39
console.log(c)

//we can use $ sign for in place of variable
var $ = 24;
console.log($);


//we can use _ sign for in place of variable
var _g = 30;
console.log(_g);